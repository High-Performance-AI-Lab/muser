[package]
name = "muser-engine-downstream-check"
version = "0.0.0"
edition = "2021"
publish = false

[workspace]

[features]
default = []
metal = ["muser-engine/metal"]

[dependencies]
muser-engine = { path = "../..", default-features = false }
