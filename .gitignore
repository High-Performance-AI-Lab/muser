# Rust build output
/target/
**/target/
/dist/
Cargo.lock.bak

# Model weights / checkpoints — never commit these
/models/
*.gguf
*.ggml
*.safetensors
*.bin
*.pt
*.pth
*.onnx

# kvpack session artifacts (local, potentially large, contain KV data)
*.kvpack
kvpack-artifacts/

# OS / editor cruft
.DS_Store
.idea/
.vscode/
*.swp
*~

# Local env / secrets
.env
.env.*
*.pem
*.key

# Logs / scratch
*.log
/tmp/
/results/
/scratchpad/
/logs/
__pycache__/
*.py[cod]

# melon-rdma feature build artifacts (compiled per-platform, never committed)
native/melon_rdma/*.so
native/melon_rdma/*.dylib
scripts/gx10/llamacpp/libmelon_rdma_pipe.so
